# このリポジトリの位置づけ（プロジェクトリーダー役）

**Why:** kanzi・stock・brain・chatと複数のアプリを並行開発しており、各アプリ担当（各サブフォルダのCLAUDE.md）は実装の詳細ルールに集中している。ここ（ルート）は、アプリ横断で見るべき共通事項・優先順位判断の置き場所として用意した（2026-09-13作成、軽いたたき台）。
**How to apply:** `app/app`直下、またはどのサブアプリで作業していても本ファイルは自動的に読み込まれる。実装の詳細は各サブアプリ自身のCLAUDE.md（`kanzi/CLAUDE.md`・`stock/CLAUDE.md`・`brain/CLAUDE.md`）が優先。ここに書くのは「複数アプリにまたがる話」「まだどのアプリのCLAUDE.mdにも書かれていない共通ルール」のみ。

---

## 1. リポジトリ構成

- `app/app`（このリポジトリ、コード本体）：`palmelo2nd/app`、public、GitHub Pagesで配信。サブアプリはそれぞれ独立した静的フロントエンド（`index.html`/`css/`/`js/`/`data/`）
  - `brain`：タスク・ナレッジ管理（Second Brain）
  - `kanzi`：漢字学習アプリ（漢検対策、開発中。App Store/Google Play申請準備中）
  - `kanzi-capacitor`：`kanzi`をCapacitorでネイティブアプリ化するためのラッパープロジェクト（2026-09-14作成）。`kanzi`の子ではなく兄弟ディレクトリ（CapacitorのCLIが`webDir`に親ディレクトリそのもの`../`を指定することを禁止するため）。詳細は[kanzi-capacitor/README.md](../kanzi-capacitor/README.md)参照
  - `stock`：投資・ポートフォリオ管理
  - `cook`：料理データベース・調理支援（食材／調理器具／料理／献立を管理。2026-09-20作成）
  - `chat`：（詳細未整理）
  - `memory`：個人用の記憶カード（フラッシュカード）アプリ。ライトナー式の間隔反復で語彙・数学記号・長文などを復習する。ストア公開は想定しない個人利用ツール（2026-09-13作成）
- `app/app_data`：`palmelo2nd/app_data`、private。各アプリのユーザーデータ（`brain/data.md`等）
- コード・データを別リポジトリに分けているのは、コードはpublic公開しつつ個人データは非公開にするため

## 2. 現状、複数アプリで共通しているルール（重複して各CLAUDE.mdに書かれがちなので、ここに集約していく）

- **コード/データ分離**：ユーザーが編集する可変データは`app_data`側、固定の参照データ・ロジックは`app`側（コードリポジトリ）に置く。詳細な線引きは各アプリのCLAUDE.mdを参照
- **オフラインファースト**：GitHub API読み書き＋LocalStorageキャッシュの二重化。失敗時はキャッシュにフォールバック
- **キャッシュバスティング**：`?v=N`をCSS/JSの参照・importに付与し、変更時は関連ファイル全体で値を揃えて+1する運用（brain・stock・kanziで同一方式）
- **README保守方針**：まとまった機能追加・変更をしたら、都度そのアプリの`README.md`に反映する
- **UI/UX設計方針（2026-09-20、cookアプリ開発で確立）**：個人利用の実用アプリとして、以下を基本方針とする。
  - 主な操作（頻出タスク）は画面移動（タブ切替）が極力少なく、ボタン操作とテキスト入力だけで完結すること
  - ボタン・入力エリアの数は必要最小限にしつつ、操作が上から下へ流れる配置にする
  - 同じような操作は同じようなエリアで行えるようにし、同じ機能のボタンはアプリ内で名称を統一する（例：「登録」「削除」等を場所によって言い回しを変えない）
  - 詳しい説明文は既定で閉じたExpanderの中に収め、通常表示はすっきりさせる
  - **改善の進め方**：闇雲に直さず、(1)そのアプリで実際に発生しそうな作業を頻度順に洗い出す→(2)最頻出の作業について、現状のUIで人間が行う操作手順を1ステップずつ書き出す→(3)各ステップの分かりやすさ・手数・迷いやすさを評価する→(4)具体的な改善案を出す、の順で1つずつ検証しながら改良する

## 3. リーダー役としての振る舞い

- 各サブアプリの担当は個別のセッション・エージェントが進める（例：kanziはkanziエージェント）。ここでの役割は実装そのものではなく、**複数アプリ間の優先順位判断・共通ルールの整理・アプリ間で矛盾が出ていないかのチェック**
- 各サブアプリのCLAUDE.mdはまだ作り込み途中のものもある（`chat`はCLAUDE.md自体が未作成）。無理に今すぐ揃えようとせず、各アプリの開発が進む中でおいおい追記していく方針
- アプリ横断の意思決定（例：複数アプリで使う技術方式の統一、共通ライブラリ化の要否）が発生したら、ここに追記していく

## 4. 未整理・今後決めること

2026-09-18、TODOはmyself/TODO.md（別リポジトリ、「app全体（横断）」セクション）へ集約した。新しい横断TODOが発生したらそちらに追記する。
